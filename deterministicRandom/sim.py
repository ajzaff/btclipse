def main():
	# number of buckets in Tried table
	numBuckets = 64
	# number of IP addresses that fit into a bucket
	numIPs = 64
	# Matrix of all IP adresses in tried table initialized to 0
	triedTable = [[0 for x in range(numIPs)] for y in range(numBuckets)]
    

if __name__ == "__main__":
    main()